info:
  build:
    artifact: zuulapp
    name: '@project.name@'
    description: '@project.description@'
    version: 0.0.1
  api-doc: http://${spring.cloud.client.hostname}${server.contextPath}/@restDocDirectory@/@restDocName@@restDocExtension@

spring:
  cloud:
    config:
      failFast: false
  application:
    name: zuulapp